@using Microsoft.Ajax.Utilities
<div class="alert alert-danger" role="alert">
    Вы допустили ошибки в указанных полях
    @*@if (ViewBag.ErrorsArray != null)
    {
        foreach (var i in ViewBag.ErrorsArray)
        {
            @Html.Raw(i.ToString())
            @Html.Raw("<br/>")
        }
    }*@
</div>
